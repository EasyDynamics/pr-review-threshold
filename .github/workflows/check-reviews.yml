# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: check-reviews
on:
  pull_request_target:
    types:
      - opened
      - labeled
      - reopened
      - unlabeled
      - edited
  pull_request_review: {}
jobs:
  Check review count:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - name: Run action
        uses: ./
